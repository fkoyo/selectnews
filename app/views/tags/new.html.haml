.newcontent
  .newcontent--box
    = form_with(model: @tag, local: true) do |form|
      %h3.newcontent--title
        新しいタグの登録をする
      = form.text_field :tag_name, placeholder: "タグの名前", class: "newcontent--content"
      = form.submit "SEND", class: "newpost-btn"